#include <stdlib.h>
#include <stdio.h>
#include <utils.h>

void printMenu() {
    printf("\n"); // options
}

int main(int argc, char *argv[]) {
    int op = -1;
    FILE *fp = NULL;

    while(/*File or stdin*/) {
        printMenu();
        scanf("%d", &op);
    }

    switch(op) {
       // option choosing
    }

    return 0;
}
