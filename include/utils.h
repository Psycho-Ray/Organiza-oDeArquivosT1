#ifndef _UTILS_H_
#define _UTILS_H_

typedef enum {
    NONE,
    READ_FROM_FILE,
    PRINT_ALL,
    SEARCH_BY_FIELD,
    SEARCH_BY_RECORD,
    SEARCH_BY_RECORD_BY_FIELD,
    EXIT
} OPTION;

typedef enum {
    NONE,
    // file organization types
} FTYPE;

#endif
